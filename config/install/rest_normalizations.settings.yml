rest_fields:
  -
    entity_type: node
    entity_fields: 'nid,uuid,vid,langcode,type,status,title,created,changed,moderation_state,type,parent_id,parent_field_name,metatag,path,default_langcode,revision_id,revision_uid,revision_timestamp,revision_log,uid,promote'
  -
    entity_type: taxonomy_term
    entity_fields: 'tid,name,description,parent,weight,default_langcode,revision_id,revision_uid,revision_timestamp,revision_log,uid,promote'
